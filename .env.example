# Optional: increases GitHub API rate limit from 60 to 5000 requests/hour
# Create a token at https://github.com/settings/tokens (public repo scope is enough)
# NEXT_PUBLIC_GITHUB_TOKEN=ghp_your_token_here
